#include <bits/stdc++.h>

using namespace std;

int main(){
	vector<string> p;
	p.push_back("lorem");
	p.push_back("ipsum");
	p.push_back("some extra lines");
	//~ cout<<p[0]<<endl<<p[1];
	int i;
	//~ for(i=0;i<p.size();i++){
		//~ cout << p[i] <<endl;
	//~ }
}	cout << typeof(p) << typeof(i) << typeof(p.size());
